{
    "name": "openapi-zod-client-examples-export-schemas-and-types-directly",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "echo 'Example - no build needed' && exit 0",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "type-check": "tsc --noEmit",
        "lint": "echo 'Example - no linting' && exit 0",
        "test": "echo 'Example - no tests' && exit 0",
        "gen:schemas-directly": "openapi-zod-client ../petstore.yaml -t schemas-and-types-directly.hbs --output petstore-schemas.ts --export-schemas"
    },
    "devDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@apidevtools/swagger-parser": "^12.1.0",
        "openapi-zod-client": "workspace:*",
        "zod": "^3.19.1"
    }
}
