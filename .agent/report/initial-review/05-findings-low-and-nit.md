# Low and Nit Findings (L1–L18, N1)

Condensed. Verification tag per entry. Most are doctrine/enforcement inconsistencies, latent code paths, or
documentation drift — correctness-neutral today.

| ID      | Finding                                                                                                                          | Location                                                                                                                                                     | Verify               | Note / fix                                                                                                                                                                                                                                                                                                                         |
| ------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **L1**  | Doctrine grants a test-`as` exception the linter forbids                                                                         | `principles.md:970` vs `eslint.config.ts` (`consistent-type-assertions: {assertionStyle:'never'}`) proven by `eslint-rules/type-assertion-policy.test.ts:84` | 🔵+🟢                | Doc says `as` is OK in tests for mocks; the rule bans non-const `as` everywhere. Reconcile the doc.                                                                                                                                                                                                                                |
| **L2**  | `no-restricted-types` bans only `Record<string,unknown>`, not the `{[key:string]:unknown}` sibling the doctrine also bans        | `eslint.config.ts:150-160`; `principles.md:923`                                                                                                              | 🔵                   | Add the index-signature form to the restricted types, or soften the doctrine.                                                                                                                                                                                                                                                      |
| **L3**  | Two **ungoverned** `eslint-disable`s in product code (no `-- JC:` justification)                                                 | `shared/openapi/version.ts:17`, `shared/doctor/preflight-validator.ts:48` (`consistent-indexed-object-style`)                                                | 🔵                   | `principles.md:1260` requires governed/justified disables; the other 4 disables have `-- JC:`. Add justifications or refactor.                                                                                                                                                                                                     |
| **L4**  | Stale "temporary" type-assertion comment                                                                                         | `eslint.config.ts:112-115` ("then restore … to `error`")                                                                                                     | 🔵                   | `typeAssertionRuleSeverity` is already `'error'`; the remediation it describes is complete. Delete the comment + indirection.                                                                                                                                                                                                      |
| **L5**  | Index-signature interfaces re-introduce `Record<string,unknown>` semantics behind a disable; the local `isRecord` accepts arrays | `version.ts:22`, `preflight-validator.ts:48-51`                                                                                                              | 🔵                   | `interface { readonly [key:string]: unknown }` is structurally the banned type under another name; `isRecord([]) === true` here. Exclude arrays; prefer the shared guard (ties into M3).                                                                                                                                           |
| **L6**  | `getOperationForEndpoint` is dead code **and** reaches into the raw OpenAPI document                                             | `context/endpoints/template-context.endpoints.helpers.ts:159`                                                                                                | 🔵 (grep: 0 callers) | Reads `openApiDoc.paths?.[…]` (the "reach back into the original doc" pattern the Cardinal Rule forbids) and has no callers. Delete it.                                                                                                                                                                                            |
| **L7**  | `McpValidationError` advertises `expected`/`received` that the formatter never populates                                         | interface `validation/mcp-error-formatting.ts:18,20`; `formatMcpValidationError:150-157` returns only `{pointer, issues}`                                    | 🔵                   | Populate from the Zod issue, or remove the fields + TSDoc.                                                                                                                                                                                                                                                                         |
| **L8**  | `additionalProperties` writer narrows `true`/schema → `false`; stale "Always emits `additionalProperties: false`" comment        | `writers/shared/json-schema-fields.ts:104,108-118`                                                                                                           | 🔵                   | **Latent** — the parser fail-fasts on `additionalProperties:true` (🟢 reproduced), so unreachable today; becomes live if the IR is built directly or parsers align. Emit faithfully; update the comment to the IDENTITY preserve-explicit policy. (Pairs with H6; this is the downgraded R7.)                                      |
| **L9**  | `booleanSchema` fail-fast message claims "no OpenAPI equivalent", inaccurate for OAS 3.2                                         | `writers/openapi/schema/openapi-writer.schema.ts:56-60`                                                                                                      | 🔵                   | OAS 3.2 _is_ JSON Schema 2020-12, where boolean schemas are valid; the JSON-Schema writer already emits them. The rejection may be a defensible closed-world _policy_, but the "genuinely impossible / no OpenAPI equivalent" rationale is factually wrong. Reword to state the policy.                                            |
| **L10** | `UnsupportedJsonSchemaKeywordError` rejection path is dead, but TSDoc still claims keywords are rejected                         | `parsers/json-schema/index.ts:230-232` (empty `UNSUPPORTED_DOCUMENT_KEYWORDS`), `@throws` claim at `:62`/`:235`                                              | 🔵                   | The set is empty ("All previously unsupported keywords are now handled"), so the error can never fire. Combined with H4, the document level has no fail-fast for genuinely unsupported keywords. Remove the dead machinery + `@throws`, or repopulate and prove.                                                                   |
| **L11** | `deriveRootName` TSDoc says "`$id` basename" but returns the full `$id` URI                                                      | `parsers/json-schema/index.ts:204` (doc) vs `:213-214` (`return doc.$id`)                                                                                    | 🔵                   | A `$id` like `https://example.com/schemas/order.json` becomes the component name verbatim. Implement basename extraction or fix the docstring.                                                                                                                                                                                     |
| **L12** | `patternProperties`-only objects escape strict closed-world enforcement                                                          | `parsers/json-schema/json-schema-parser.object-fields.ts` (`isObjectKeywordCandidate`)                                                                       | 🟢 ran code          | `parseJsonSchema({patternProperties:{'^x-':{type:'string'}}})` → IR `type:undefined, additionalProperties:undefined` (open), unlike properties-bearing objects which get `additionalProperties:false`. Include `patternProperties`/`propertyNames` in object-keyword detection.                                                    |
| **L13** | Grouped-determinism test compares only path keys, not file bodies                                                                | `rendering/templating.unit.test.ts:185-187`                                                                                                                  | 🔵                   | Asserts `firstRun.paths === secondRun.paths` (sorted key lists) but never `firstRun.files === secondRun.files`; non-deterministic _content_ would pass. Add `expect(firstRun.files).toEqual(secondRun.files)`.                                                                                                                     |
| **L14** | Draft-07 converter silently remaps `unevaluatedProperties` → `additionalProperties` (semantic change)                            | `conversion/json-schema/keywords/keyword-object.ts:137,139`                                                                                                  | 🔵                   | `unevaluatedProperties` is composition-aware; `additionalProperties` is local-only — not equivalent. Internal/test-facing converter (not on the canonical pipeline), so Low. Fail-fast on the downgrade or document it as lossy best-effort.                                                                                       |
| **L15** | Bundle metadata captures `new Date()` + `process.cwd()` (latent determinism risk)                                                | `shared/load-openapi-document/bundle/bundle-infrastructure.ts:57` (`capturedAt: new Date().toISOString()`); `bundle-metadata.types.ts:69`                    | 🔵                   | The only `new Date()` in product code. Diagnostics-only today, so Low; if this metadata is ever serialised to output, two runs diverge. Inject a clock or drop time/absolute-path from any output-bound metadata.                                                                                                                  |
| **L16** | Array-valued `items` degrades to `z.unknown()` (latent)                                                                          | `writers/zod/generators/collections.ts:38`                                                                                                                   | 🟡 mechanism         | When `schema.items` is an array, the `else` branch emits `z.unknown()`, discarding positional schemas. Normalisation converts array `items` → `prefixItems`, so likely unreachable; assert/throw rather than widen if it ever occurs. (Distinct from the _absent-items_ case, which is correctly `z.array(z.unknown())` — see R2.) |
| **L17** | Governed but glib `no-unused-vars` disable                                                                                       | `shared/maybe-pretty.ts:13` (`-- JC: it really is a pain to do it properly`)                                                                                 | 🔵                   | Destructure-to-omit `plugins`; the disable is permitted (user-added) but `principles.md` calls unused-vars exceptions "NONE." A typed `omit` helper would avoid it. Nit.                                                                                                                                                           |
| **L18** | `ajv-draft-04` imported in `lib` test but declared only in root `package.json`                                                   | `tests-snapshot/spec-compliance/openapi-spec-compliance.test.ts:20`; root `package.json:56`; absent from `lib/package.json`                                  | 🔵                   | Strict-letter `principles.md` §8 ("declare what you import in the consuming manifest"). It is genuinely used (so not dead — see R6) and `knip` ignores it for sub-path-resolution reasons. Nit; add to `lib/package.json` devDeps for honesty.                                                                                     |
| **L19** | CLI silently drops invalid `--group-strategy` / `--default-status` (coerced to `undefined`, no error)                            | `cli/helpers.ts:54,59`                                                                                                                                       | 🔵                   | A value the user explicitly set but mistyped is ignored silently. Distinguish "not provided" from "provided-but-invalid"; throw an actionable error listing allowed values. (`--complexity-threshold abc` → `NaN` is moot — the option is unused; see H5/R8.)                                                                      |
| **N1**  | TypeScript type writer widens `enum`→`string`, `const`→`string`, `['string','number']`→`unknown`                                 | `writers/typescript/type-writer/core.ts:52,68,82`                                                                                                            | 🟢 ran code          | Reproduced (`getTypescriptFromOpenApi`). Real type-fidelity loss **but** reachable only from `tests-snapshot` (not the public API, not the primary Zod path), so Low-reach. If the TS type writer becomes public, emit literal unions / fail-fast; mirror the JSON-Schema writer's `enum` handling.                                |
