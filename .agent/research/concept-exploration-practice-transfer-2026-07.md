# Transfer entries — concept exploration skill (July 2026)

Entries per the upstream Practice's `practice-change-transfer-report` rule; portability classes
per the upstream PDR-136 taxonomy. Neither surface is installed in castr yet — both are named
here as upstream provenance, not as resolvable cites.

## Vendor-agnostic concept exploration skill

- **What changed** — Promoted the four-movement concept-exploration workflow into
  `.agent/skills/concept-exploration/SKILL-CANONICAL.md`, generated vendor adapters, added the
  invocation permission, and routed raw or prematurely framed questions from the principles
  surface into the skill.
- **Why** — Recurring phenomena and messy observation sets need an explicit framing discipline
  before formed questions reach `reason` and the decision lenses; without it, fluent option lists
  can foreclose the real question.
- **Replication path** — Port the canonical skill and this note, regenerate adapters with the
  receiving estate's vendor prefix, add the local invocation permission and principles pointer,
  then prove a raw-observation case and a formed-options near miss before running the estate's
  portability and skills checks.
- **Portability class** — `portable substrate` for the canonical workflow; `configuration` for
  adapter names, permission entries, and the local principles pointer.

## Provenance and local adaptation

The skill promotes the four-movement workflow from the
`baxtersgallery/research/concept-exploration.md` prototype in the resonance repository (not
brought to castr — provenance only). It retains the alternating reflection and synthesis
movements while replacing prototype-specific framing with the Practice's canonical
`metacognition`, `reason`, planning, and portability routes.

## castr transfer entry (2026-07-18)

Second hop: resonance → castr, per the replication path above. The canonical skill and this note
were ported; adapters regenerated with castr's `engraph-` prefix; the
`Skill(engraph-concept-exploration)` permission added; the principles pointer localised into
`principles.md` § Decision Lenses. castr has the lenses and the `PDR-057` § Four-Lens dissolution
test but not the upstream `decision-matrix` rule, so the routing block and this skill's Routing
Boundaries feed the lenses directly. A worked concept-exploration run pre-dates this install in
castr: `remediation-program-concept-exploration-2026-07-17.md`, which lands in this directory
with PR #10 (the remediation program record) — cross-link it to this skill once both branches
merge (named follow-up in the transplant plan).
The two-case proof for this transfer (raw-observation firing; formed-options near miss) is
recorded in the transplant plan
(`../plans/transplant/resonance-practice-imports-2026-07.md`).
