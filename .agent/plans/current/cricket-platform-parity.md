---
todos:
  - id: cricket-tooling
    content: Enforce platform-specific adapter contracts through generation and validation.
    status: completed
  - id: cricket-install
    content: Install Cricket definitions, correct installation docs and prove the native panel.
    status: in_progress
---

# C05 Cricket platform parity

Owner-requested bounded execution under the
[correction commission](../active/castr-documentation-and-fidelity-correction.md).
Original shepherd: Moon turns Apogee, Codex gpt-6-astra, session prefix 01a076,
identity a9c4176b-2675-5890-bf98-420f545b146e; started 6 September 2026.
Parent plans, ledger, manifests and shared continuity remain coordinator-owned.

## Current integrated delivery — 9 September 2026

Bora seeks Turbulence now owns the remaining installation and its repository
delivery. PR #88 merged the platform contract as
`bd115294cd545db6d6e64f6c4629a5a583e5ded3`. PR #90 then merged the complete
PR #28 preservation disposition as `cb73c4deaae9eecdbe406f28304314c71e339e02`,
and PR #28 closed without merge while its recovery branch remained intact. The
opening condition for this installation is therefore satisfied.

Branch `codex/cricket-installation` merged that current main at
`7967ff92479697610bda30c69ffca0b7071941d3`. Its remaining delta is the Cricket
templates, registrations, generated adapters, adjacent operating documentation,
retained execution evidence and one narrow generator-source correction for Cursor
Cricket disclosure. PR #88's delivered platform-contract tooling is no longer part
of this changeset. On the integrated tree, `pnpm agents:check`, portability and
subagent validation pass; the installed inventory is 17 templates, 21 Codex roles
and 22 Claude/22 Cursor wrappers; 120 focused adapter, registry and portability
tests pass. Configuration review exposed that generated Cursor discovery
descriptions implied a runtime effort pin; the generator now states that Cursor
preserves the stable method role while leaving reasoning effort unpinned, and all
four Cursor Cricket wrappers were regenerated. Existing behavioural tests prove
the absence of Cursor model, effort and tool pins; human review owns the wording.
Paired documentation and onboarding review passed on the integrated opening
candidate. The canonical `pnpm check` then completed with exit 0: 1,718 Castr
tests, 1,875 agent-tools tests, ten Castr E2E tests and 159 agent-tools E2E tests
passed, including the 158-case adapter checker. Commit and full pre-push hooks
passed at `3b568416`, and [PR #91](https://github.com/EngraphCode/castr/pull/91)
opened from that head. Its review waves found compiled-procedure intake,
decision-table and output-contract contradictions, incomplete routing descriptions
across all Cricket roles, and stale pre-opening instructions across the current routes.
The active candidate repairs the complete classes at their canonical sources, including
fail-fast invalid-stance handling across both methods, an explicit complete-frame guard,
and a satisfiable brevity contract that cannot silently truncate required evidence.
Exact-head re-review, checks and merge remain required.

The retained native record establishes I2 at its stated boundary: three exact
registered roles were dispatched with `fork_turns: "none"`, and the same three
agents returned the adversarial follow-ups. The record embeds its tested Codex
adapters and dated template outputs; the current candidate retains the invocation
boundary while correcting descriptions and the compiled output contract. A fresh full panel was
requested from the existing trusted Castr task because Bora's older task schema
cannot reload newly installed roles. The [dated attempt record](../../reports/agentic-engineering/cricket-castr-integrated-boundary-undelivered-2026-09-09.json)
preserves both exact requests, turn identifiers and the task's terminal messages:
`Session stopped.` and `Session remains closed.` No registered-role dispatch,
follow-up or Cricket return is observable, so all six requested returns are
`UNDELIVERED`. No generic agent substitutes for them. This delivery failure does
not invalidate the retained I2 invocation proof because registered role names,
bindings, model/effort/tool pins, dispatch and same-agent follow-up remain unchanged.
Subsequent changes correct routing descriptions and the compiled procedure's output
contract; exact-source review and generated validation cover those semantics, while
the retained native run remains evidence only for its dated template revision.

Parallax revision cricket/3 and the proportionality check keep one coherent
installation outcome. Splitting current authority from the definitions would
create an installed panel whose entry routes still point to completed transitions;
combining unrelated compiler or source-PR extraction work would broaden the story.
The next value boundary is therefore one reviewed installation PR, current-head
checks and merge. PR #14 follows after that delivery.

Current acceptance state:

- T1 and T2 are delivered by PR #88.
- I1 passes on the integrated tree through generator comparison and strict roster
  validation.
- I2 is established by the retained six-return native record at its stated scope;
  the current-boundary rerun is recorded `UNDELIVERED` because its host task exposed
  no observable role dispatches or returns.
- D1's documentation/onboarding review and the integrated working-candidate
  aggregate pass; PR #91 is open and repository delivery remains pending its
  current-head review/check harvest, any resulting fixes and merge.

The execution record below is historical evidence. Status statements in it describe
the observed boundary at the time and do not override this section.

## Resumed execution — 6 September 2026

Jim resumed implementation with workspace-write permissions. Current shepherd:
Kite hunts Eyrie, Codex gpt-6-astra, session prefix 01a077,
identity e228cbb1-0ab0-5663-b0e1-b0a0f19c7939. Moon's original identity and review
history remain above and below. Bora confirmed the same bounded custody and no
competing gate run. Claim `206ff250-91ae-4c0e-9923-6a22902a116a` covers this repair.

Baseline: 111 focused tests passed; two checker tests failed on the missing
`compareAdapters` export. Package type-check failed on that same missing export.
No inherited green aggregate is claimed. Tooling work is split between strict
registry parsing, generator validation and checker/CLI integration, with root
integration and required specialist reviews. Installation and native panel proof
remain the second slice. Re-ground and refresh main before each PR boundary.

Review history: the inherited gateway implementation feedback is round 1 and
remains open until its class-level repairs and verification are complete. The
first PR's REVIEW-TALLY must preserve this batch; resumption does not reset it.

### Resumed 9 September 2026

Jim explicitly approved the previously blocked native-context transfer. At this
resumption the task's live collaboration schema already exposed all three exact
Cricket roles, so direct native dispatch became available. No external CLI process
was launched. Three normal returns and adversarial follow-ups from those same
three agents are retained in the
[native invocation record](../../reports/agentic-engineering/cricket-castr-native-2026-09-09.json).
This observed role-loading boundary replaces the earlier unproven requirement to
start a separate process; no claim is made that this was a new task identifier.
I2's native invocation and semantic spot check are now established for this candidate.
The combined candidate's canonical `pnpm check` completed with exit 0 on 9 September.
Tooling commit `27f90f18171ac3ab4216a618deff9f03be928915` contains 45 files;
its commit hooks passed. Its isolated `codex/cricket-platform-parity` worktree
also passed full pre-push `pnpm check:ci` with observed exit 0, remained clean and
was pushed. [PR88](https://github.com/EngraphCode/castr/pull/88) is open with its
[preserved review tally](https://github.com/EngraphCode/castr/pull/88#issuecomment-5600132801).
The dependent installation is on `codex/cricket-installation`. Both PR deliveries
remain pending. The tooling-only result now establishes the independent split;
the earlier combined candidate result did not establish it.

Both judgement roles returned ON-TRACK in both stances. The procedure returned
DRIFTING in both, asking for first-hand support for historical review/test claims.
Accepted: retain source evidence and obtain fresh required gate results. Not adopted:
repeating settled reviews simply to obtain agreement. The
[historical reviewer/worker record](../../reports/agentic-engineering/cricket-castr-review-evidence-2026-09-06.json)
preserves the actual returned reports. Encrypted message bodies in the session log
prevent automated byte-equality readback of the supplied frames; the dispatch
record explicitly states that limit rather than treating ciphertext as a frame.

The two remaining documentation concerns were closed and rechecked by docs-adr-expert:
direct source attribution and accurate public API contracts, including real resolver
examples and role-dependent settings defaults. The
[final review record](../../reports/agentic-engineering/cricket-castr-final-review-evidence-2026-09-09.json)
retains returned reports and the release review's remaining conditions. The
6 September canonical-check log reaches successful final E2E results, but its
process exit handle expired; the observed 9 September exit 0 supplies current
candidate evidence. Its build, format, type, lint, dependency, portability,
packaging, validator and full test stages passed; cached successes are included.
The by-file link report lists 173 inherited broken links across 798 files, with
none in the changed Cricket documents. This informational report is not a clean
repository link claim. Main was fetched and remains `6c3b18f0`.

Release review found no additional implementation blocker. It required retention
of final review closure, independent tooling verification, installation validation
against its delivery base and fresh PR-surface checks. There was no retained final
gateway closure, so a bounded recheck of its accepted findings was requested.
That recheck returned APPROVED on `27f90f18`, verified in the clean tooling
worktree, with no remaining concrete issue. Its actual return is retained in
the final review record; specialist compliance is not substituted for a gateway
verdict. Closure verification does not begin a new feedback round.

The final documentation pass narrowed its remaining concerns to an unregistered
resolver example and inaccurate settings-default TSDoc. This is review round 3,
so the tooling tally records PDR-132 budget-exceeded. Scope was reconsidered:
one validated role contract drives the parser, generator and checker, and splitting
those would leave inconsistent intermediate contracts. Keep the independent
tooling unit and the separate installation unit. Nineteen generated-file changes
follow the same producer. The third round adds no product story; it repairs
executable examples and public contract descriptions. The retained rounds and
cricket/2 structural step-back are carried into the PR tally without a reset.

The first commit's pre-stage guard rejected a malformed claim pattern:
`area-kind git` requires `area-pattern index/head`, not `git:index/head`.
The subsequent commit workflow independently completed with intact hooks, but
continuing after the guard failure was an orchestration error. The malformed
claim is closed with the landed SHA. Future staging requires a successful guard
with the corrected pattern; the completed queue intent is not replayed.

## Outcome and mechanism

Castr contributors can run the documented Cricket panel from Castr's registered
roles. A shared explicit platform contract connects generated adapters to strict
validation; installing definitions then enables the native runtime proof.
Ordinary reviewer requirements remain intact. No compiler or unrelated Practice
imports belong here.

Parallax core, revision cricket/1, same-context inquiry with independent specialist
reviews sharing the source repositories: the counterframe is to copy missing
files alone. Rejected because current generation and validation require a symmetric
roster and high effort, while the verified source contract requires three Codex
seats and four Claude/Cursor seats. Proportionality separates tooling from installation
and runtime proof. Reopen the split if either slice cannot remain independently green.

Source basis: OCE host 92d4854b4465238ecca8e7858527b98a3ea08853, refreshed on
6 September; its skill, two templates and three Codex adapters agree with source
31e76a7237ee7aecb8adfca96e73b2d83b25be39. This is source evidence, not Castr
runtime evidence. Configuration pins never prove model self-reports.

### Owner-directed reflection — cricket/2, 6 September 2026

**Status: provisional.** This revision supersedes cricket/1's proof ordering,
while preserving that earlier reasoning above. Charter: restore a contributor's
documented Castr invocation through registered roles and substantive returns.
Jim owns scope; Kite owns the bounded implementation; Bora owns shared delivery
records. No compiler work or judgement-reliability benchmark enters this slice.

Envelope: `inquiry_id: castr-cricket-restoration`; `inquiry_revision: cricket/2`;
`basis_id: registered-invocation`; `scale_region: one-castr-candidate`;
`method_pass_id: direction-and-boundary-review-2`; producing skill Parallax 0.1.0,
core depth, profiles investigation + software-engineering. Core is proportionate:
one serious alternative and a reversible native probe resolve the live uncertainty.
Execution is parent synthesis plus a separate assumptions-expert context sharing
the same repository sources; it is analytical independence, not empirical replication.
Inputs are the uncommitted candidate based on `3ad2ab5`, the preserved handover,
the current user reflection request, local failing/passing probes, gateway reports,
and the six OCE source files verified against `31e76a7`. Permissions permit local
implementation; a fresh execution-service transfer is awaiting explicit approval
after automatic approval review rejected its private-context payload.

The exploration changed an inherited assumption. Initially the repair was framed
as tooling parity followed by an installation PR after the tooling merge. The
actual harm is a contributor being unable to run the documented direction panel.
Copying definitions alone leaves generation/validation inconsistent, but no observed
runtime requirement makes a merge necessary for candidate role loading. Therefore
prepare the installation and native proof alongside tooling review, before the
relevant merge. The two coherent changesets remain useful review units, not runtime
prerequisites. A reviewer independently reached the same sequencing correction from
the shared sources; that agreement does not multiply the evidence.

Bridge claim: accepted configuration and generated wrappers can enable a native
registered invocation. Static source agreement is necessary but insufficient; the
bridge fails on absent role discovery, rejected admission, incorrect template/model
configuration, failed same-agent follow-up, or returns that do not perform the task.
Crosswalk: three Codex roles project to four Claude/Cursor wrappers; that explicit
asymmetry preserves methods, not equivalent runtime or model behaviour across hosts.
Six substantive returns prove invocation and a limited semantic spot check on this
candidate, not reliable judgement quality. Normal/adversarial ordering and retained
agent history also prevent attributing differences solely to stance.

World return: Kite obtains exact dispatches, configured bindings, three returned
identifiers, three normal returns and three adversarial follow-ups to those same
agents. Inspect each return's meaning. Missing or generic acknowledgements keep I2
undelivered. Repeat the full panel at a materially changed delivery boundary, not
on an unchanged frame or a timer. Preserve exact platform failures without substitutes.
Complete deterministic gates and specialist reviews; stop this inquiry when the
documented invocation and release evidence hold, or reopen it on a named falsifier.
The pending runtime observation is the material uncertainty, so further general
analysis has lower value than the authorised probe once its approval is resolved.

Review round 2 narrowed the inherited failures to valid multiline/scalar-like
descriptions producing invalid YAML, metadata falsely satisfying an instruction,
an optional ordinary model bypassing type validation, and weak test fixtures.
All are accepted as correct, relevant and proportionate boundary repairs. Generator
serialization recorded 5 red / 71 green before repair, then 76 green. Metadata/body
cases were red before the body-only fix. Ordinary model cases recorded 4 red / 8
green, then 20 green with adjacent validator tests. Configuration review found no
gate bypass; security review found no additional practical blocker. Test-quality
repairs remain in progress; no canonical aggregate is claimed for this tree.

Learning signal for coordinator integration: cricket/2 exposed a merge dependency
that the runtime mechanism did not require. Expected: merging tooling unlocked proof;
observed: installing a local candidate is the prerequisite, while service-transfer
approval is a separate boundary. Confidence is high for the sequencing distinction,
pending for actual runtime success. Consider this single case during later workflow
distillation; it does not justify a global skill or memory change now.

## Execution and proof

1. Tooling PR, code/configuration class: write failing tests for platform-aware
   generation, supported/missing/unsupported roles, exact model/effort/method
   validation and misleading nested/instruction-body fields. Repair existing
   generator, checker, portability and subagent validators, including Claude
   validation. Preserve configured model metadata through the resolver.
   Describing surface: adapter text produced/accepted/rejected by these public
   tooling boundaries. Unit/integration proof uses the existing agent-tools suite.
2. Dependent installation PR, configuration/documentation class, based on tooling:
   install two canonical templates, three Codex roles and generated Claude/Cursor
   quartets; correct adjacent installation instructions and source limitations.
   Proof: generated parity, exact binding inspection, and a fresh trusted Castr
   session with three normal then three adversarial returns from the same agents.
   Establish this candidate proof before the relevant merge, alongside tooling review.
   Missing native returns remain UNDELIVERED; no fourth Codex seat or fallback.

Both slices follow [PDR-132](../../practice-core/decision-records/PDR-132-changeset-health-round-budgets-bind-at-authoring-time.md).
Review tally begins at first triage and is carried into the PR comment. Gateway,
configuration, testing and paired documentation/onboarding reviews apply to their
surfaces. Re-ground at each PR boundary. Preserve hooks and merge by merge commit
only after full current-head reviews, comments, threads, checks and ruleset findings.

Acceptance: T1 generated contracts and negative validation tests pass; T2 ordinary
reviewer restrictions remain proven; I1 exact installed platform parity passes;
I2 six substantive native Castr returns are retained with dispatch and same-agent
follow-up evidence, as invocation/conformance proof plus a semantic spot check;
D1 documented
installation is executable and limitations accurate. Run focused tests, formatting,
portability, repository validators, by-file link report and canonical `pnpm check`.
New public logic gets TSDoc and adjacent tooling README documentation; no new ADR
is needed for restoring the already-authorised platform contract.

Report each merged slice and exact evidence to the coordinator for shared-record
integration. Full C05 Cricket acceptance stays pending until all criteria hold.

## Owner-directed transfer — 6 September 2026, 13:56 UTC

Jim stopped implementation to move it to a fresh session with workspace-write
permissions and explicitly requested that all intentions, plans and continuation
instructions be saved. The host supplied read-only sandbox policy despite explicit
editing authority; no available task-management tool could change it. Do not
describe this transfer as a green closeout or repeat per-PR permission waits.

HEAD is still `3ad2ab5dbff006786394dd7689e892be412c1687` on
`codex/cricket-platform-parity`, originally created from fetched main.
**All work is uncommitted, including untracked files. No push, PR or merge exists.**
Preserve this worktree. The
[continuation prompt](../../prompts/cricket-platform-parity-continuation.md)
is the fresh-session entrypoint. Reconcile this bounded plan's lifecycle placement
at resumption without competing with the primary correction plan.

### Implemented but unfinished

- Installed dependencies and built agent-tools via bootstrap; Husky remains active.
  Added direct `smol-toml` dependency resolving 1.8.0.
- Added `agent-tools/src/core/reviewer-adapter-platform-contract.ts`: exact Cricket
  mappings, Codex high-seat exclusion, complete-roster calculation.
- Added `agent-tools/src/core/toml-document.ts`; replaced regex parsing in
  `core/codex-project-agent-registry.ts`,
  `validators/subagents/validate-subagents-codex-toml.ts` and
  `validate-subagents-codex-instructions.ts`. Real TOML is now used, but parser
  strictness is unfinished.
- Added nullable configured `model` in `core/codex-project-agents.ts` and CLI
  reporting in `bin/codex-reviewer-resolve.ts`. Null denotes inheritance, not an
  observed model. Runtime proof remains outstanding.
- Updated Codex validation to enforce named Cricket model/effort/method, reject
  fourth Codex seat, retain ordinary high effort and safety.
- Updated portability for three/four roster. Generator now checks registration
  existence, omits Cursor Cricket pins and all unsupported Cursor tools, emits
  Claude Cricket bindings/tools and adds the Claude/Cursor-only high seat.
  **Generation has not run**; existing generated Cursor files still have tools
  fields rejected by the new validator.
- Added strict YAML Markdown validator and 35 tests in
  `validators/subagents/frontmatter-schema.ts` and `frontmatter-schema.test.ts`.
  Updated `validate-subagents.ts` to check Claude as well as Cursor, including
  template existence/coverage and components.
- Added `tests/agent-adapter-generate/cricket-contract.unit.test.ts`,
  `checker.unit.test.ts`, `tests/toml-document.unit.test.ts`. Corrected old
  `generator.unit.test.ts` fixture paths and partial registries after their failures.
- No Cricket templates/roles/registrations/adapters have been installed. Canonical
  Cricket skill and installation docs remain unchanged. No native panel has run.

### Actual verification

| Observation                     | Result and scope                                                                                                                   |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Initial new Cricket test        | 19/19 failed on original tooling: uniform effort, accepted wrong models/methods, platform roster, Cursor pins and missing adapters |
| Combined focused run, 13:54 UTC | 63 passed, 4 failed across five files; all 19 Cricket tests passed                                                                 |
| Four combined-run failures      | Old generator fixtures used wrong .codex/.codex paths and incomplete adapter maps; corrected afterwards, not rerun                 |
| Checker test red, latest run    | 2/2 failed: compareAdapters is not a function; checker implementation untouched                                                    |
| TOML-document tests             | 9 passed; added after initial implementation, supplementary regression evidence rather than claimed red/green                      |
| Markdown worker                 | Reported 35 tests, formatting and scoped ESLint passed for three owned files                                                       |
| Worker package type-check       | Reported four TomlTable overload errors; parent changed parameter to TomlTable afterwards, not rerun                               |

No canonical aggregate, final type-check, full validators, portability, link report,
generation or final review has passed for this dirty tree. Built tools may lag
source. No result from another worktree proves this one green.

Commands already used:

```bash
pnpm --filter @engraph/agent-tools exec vitest run tests/agent-adapter-generate src/validators/subagents/validate-subagents.unit.test.ts src/validators/portability/validate-portability.unit.test.ts tests/codex-project-agents.integration.test.ts
pnpm --filter @engraph/agent-tools exec vitest run tests/agent-adapter-generate/checker.unit.test.ts tests/toml-document.unit.test.ts
pnpm --filter @engraph/agent-tools exec vitest run src/validators/subagents/frontmatter-schema.test.ts
```

### Outstanding review and implementation

Gateway verdict: **CHANGES REQUESTED**. The initial architecture/configuration/
proportionality reviews informed design; the gateway's first implementation
feedback batch is now pending response. Carry these dispositions into the first
PR's actual REVIEW-TALLY. No second narrowing round has yet occurred.

1. **Validate adapters before generating.** `buildAgentRoster` validates
   registrations but never calls `getCodexAdapterValidation`. Add generator
   rejection tests for mismatched identity/description, wrong model/effort/method,
   weak ordinary safety and forbidden Codex fourth seat. Validate before projection,
   check config_file/name agreement, require complete supported rosters.
2. **Make registry parsing strict.** Gateway directly reproduced three holes in
   `toml-document.ts`: max_threads = -2.5 accepted/ignored; unknown registration
   model key silently stripped by z.object; agents.max_threads table with
   description/config_file admitted as a fake role. Separate reserved setting
   names from role names before value parsing; reject unknown registration keys
   and wrong shapes. Verify vendor numeric constraints before enforcing them.
3. **Finish checker TDD.** Implement the comparison boundary required by
   `checker.unit.test.ts`, connect it to real generated-estate enumeration in
   `checkAdapters`, reject missing/drifted/surplus files, update CLI reporting
   and exit conditions. Do not leave comparison as a test-only helper.
4. **Complete proof.** Add generator high-seat output proof, ordinary reviewer
   regression, duplicate/malformed/nested/prose negatives, multiple-template and
   registration mismatch cases, and resolver configured-model preservation.
   Use meaningful tests at actual boundaries. Existing runtime integration tests
   use filesystem IO; do not blindly expand that inherited taxonomy violation.
5. **Polish before final review.** Remove stale regex/triple-quoted-only comments
   and messages. Update parity/generator docstrings for asymmetry. Add TSDoc and
   adjacent agent-tools README documentation. Review repeated parsing and optional
   lookups for simpler boundaries. Format, type-check, lint, regenerate and inspect.
6. **Finish tooling slice.** Gateway/config/type/test review, paired docs/onboarding
   for significant docs; canonical gates and by-file link report. Re-fetch main,
   preserve dirty work, commit with intact hooks, push, harvest every feedback
   surface and ruleset finding, maintain tally and conditionally merge by merge commit.
7. **Then install definitions/docs.** Two templates, three Codex definitions and
   registrations, eight generated Markdown wrappers. Correct canonical skill's
   Oak wording and missing method/evidence provenance limitation. Correct
   `.agent/sub-agents/README.md`, `.agent/directives/AGENT.md`,
   `.agent/memory/executive/invoke-code-experts.md` and
   `.agent/memory/executive/cross-platform-agent-surface-matrix.md`.
   Expected installed counts: 17 templates, 21 Codex roles, 22 Claude/22 Cursor;
   verify actual inventory. Some current docs already falsely say wrappers absent.
8. **Native proof still uncertain.** Fresh trusted Castr session must expose the
   three registered roles; current session schema does not. Establish supported
   native/CLI fresh-session route and retained reports path before claiming I2.
   Preserve loaded role schema, exact config, three returned agent identifiers,
   normal returns and adversarial follow-ups to those same agents. Luna/xhigh
   admission failure stays explicit. No generic fallback or fourth Codex seat.

### Refreshed source contract and hazards

OCE host was `92d4854b4465238ecca8e7858527b98a3ea08853`. Parent directly checked
the six-file diff against `31e76a7237ee7aecb8adfca96e73b2d83b25be39` at transfer:
empty. Source is [OCE](https://github.com/EngraphCode/open-curriculum-ecosystem).

| Role                     | Template             | Codex                  | Claude        | Cursor   |
| ------------------------ | -------------------- | ---------------------- | ------------- | -------- |
| cricket-judgement-low    | cricket-judgement.md | gpt-5.6-sol / low      | fable / low   | unpinned |
| cricket-judgement-medium | cricket-judgement.md | gpt-5.6-terra / medium | opus / medium | unpinned |
| cricket-judgement-high   | cricket-judgement.md | unsupported            | sonnet / high | unpinned |
| cricket-procedure-xhigh  | cricket-procedure.md | gpt-5.6-luna / xhigh   | haiku / xhigh | unpinned |

Claude Cricket allows Read, disallows Write/Edit/Bash/Grep/Glob, waives
reading-discipline grounding while preserving identity. Cursor is readonly with
no model/effort fields. Templates permit two targeted verification reads; grounding
reads are outside that budget. OCE historical tally links do not exist in Castr:
retain accurate source attribution without fabricating local evidence.
OCE has no current Castr-shaped agent-adapter-generate directory; adapt contract
semantics rather than copying an assumed generator. Avoid importing broad unknown
schema fields or blanket Cricket-prefix exemptions.
Use the current six fields: OBJECTIVE FRAME, CRITICAL-PATH OWNER, INTENT,
RECENT ACTIONS, NEXT, STANCE. Older memory checkpoint labels are superseded.

### Coordination and transfer custody

Commissioner: `01a0722e-0cec-7cf1-a67b-a8787ac9b2d0` (Identify session repository).
It accepted this parallel C05 scope and owns PR85, operation-security-empty, parent/
broad plans, ledger, manifests and shared continuity. Send evidence for integration,
do not edit those records. Its PR85 pre-push passed at
`1da974f5c907bc8d08eafa381ebeac7f2dcaa0a3` and freed the expensive-gate window;
that is historical coordination evidence. Coordinate again before an aggregate/push.

Local claim `690fe396-0d02-495f-9780-3c4555c74a3c` was explicitly closed and
archived for transfer; the collaboration CLI confirmed success.
Inspect actual status at resumption and open a new identity/claim. No commit queue,
git-window claim, automation or running test process is owned. All subagents have
finished; no further edits are delegated. Coordinator retains shared-record custody.

No global continuity/consolidation or per-user memory edits were made. Session
lesson for coordinator: owner authorisation and host sandbox policy are separate;
check the actual coding permissions at task creation. Jim's transfer request
preserves the Cricket objective. Reopen this same dirty worktree to continue.
