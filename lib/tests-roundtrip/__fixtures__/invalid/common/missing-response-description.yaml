# INVALID: Response missing required description field (required in both 3.0.x and 3.1.x)

openapi: '3.0.3'
info:
  title: Invalid API
  version: '1.0.0'
paths:
  /items:
    get:
      operationId: getItems
      responses:
        # Response 200 is missing required 'description' field
        '200':
          content:
            application/json:
              schema:
                type: string
