components:
  schemas:
    Pet:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string
        tag:
          type: string
    PetList:
      type: array
      items:
        $ref: '#/components/schemas/Pet'
