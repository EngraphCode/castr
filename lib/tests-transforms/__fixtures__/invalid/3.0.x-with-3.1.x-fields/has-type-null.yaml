# INVALID: OpenAPI 3.0.x with type: 'null' (3.1.x only)
# In 3.0.x, type must be one of: array, boolean, integer, number, object, string
# The value 'null' is NOT allowed in 3.0.x type enum

openapi: '3.0.3'
info:
  title: Invalid API
  version: '1.0.0'
paths:
  /items:
    get:
      operationId: getItems
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvalidSchema'

components:
  schemas:
    InvalidSchema:
      type: object
      properties:
        # type: 'null' is NOT valid in 3.0.x
        nullValue:
          type: 'null'
