# Edge Case Fixtures

Minimal, focused fixtures for testing specific edge cases.

## Planned Fixtures

- `nullable-types.yaml` — OAS 3.1 `type: [string, null]` handling
- `circular-refs.yaml` — Self-referencing schemas
- `composition.yaml` — allOf/oneOf/anyOf combinations
