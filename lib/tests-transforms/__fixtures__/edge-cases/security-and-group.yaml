openapi: 3.1.0
info:
  title: Security AND-Group Fixture
  version: 1.0.0
security:
  - apiKey: []
    oauth2:
      - read
paths:
  /things:
    get:
      operationId: getThings
      security:
        - apiKey: []
          oauth2:
            - read
      responses:
        '200':
          description: ok
components:
  securitySchemes:
    apiKey:
      type: apiKey
      name: X-API-Key
      in: header
    oauth2:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://auth.example.com/authorize
          tokenUrl: https://auth.example.com/token
          scopes:
            read: Read access
